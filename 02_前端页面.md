# 公司内部内容自动发布平台 — 前端页面设计

## 1. 前端原则

前后端完全解耦。

前端只负责：

- 登录交互
- 页面展示
- 表单操作
- 内容上传
- 调用 REST API
- 展示审核状态
- 展示发布状态
- 内容检索
- 内容跳转 / 查看

前端不得：

- 直接访问服务器物理目录
- 自己决定发布目录
- 自己执行文件发布
- 在前端保存服务器路径配置

所有发布行为统一由后端 API 完成。

---

# 2. 页面总览

第一阶段建议实现 11 个主要页面。

| 页面 | 管理员 | 普通员工 |
|---|---:|---:|
| 登录页 | ✓ | ✓ |
| 首页 | ✓ | ✓ |
| 内容列表 | ✓ | ✓ |
| 新建 / 编辑内容 | ✓ | ✓ |
| 内容详情 / 预览 | ✓ | ✓ |
| 审核列表 | ✓ | - |
| 审核详情 | ✓ | - |
| 发布配置 | ✓ | - |
| 用户管理 | ✓ | - |
| 日志 | ✓ | - |
| 内容检索 / 查看 | ✓ | ✓ |

部分新增 / 编辑操作可以使用 Drawer / Dialog，而不需要增加独立页面。

---

# 3. 页面详细说明

## P01 登录页

路由建议：

```text
/login
```

功能：

- 用户名
- 密码
- 登录
- 登录失败提示

登录成功后调用：

```text
GET /api/auth/me
```

获取当前用户角色。

---

# 4. P02 首页

路由：

```text
/
```

管理员显示：

- 内容总数
- 待审核数量
- 已发布数量
- 发布失败数量
- 最近提交
- 最近发布

普通员工显示：

- 我的内容数量
- 待审核数量
- 已发布数量
- 已驳回数量
- 最近编辑内容
- 最近发布内容

页面主要用于快速进入：

- 新建内容
- 我的内容
- 待审核
- 内容检索

---

# 5. P03 内容列表

路由：

```text
/contents
```

管理员：

- 查看全部内容

普通员工：

- 默认只查看自己创建的内容

筛选：

- 关键词
- 内容类型
- 分类
- 审核状态
- 发布状态

列表字段：

```text
标题
类型
分类
创建人
审核状态
发布状态
更新时间
操作
```

管理员操作：

- 查看
- 编辑
- 删除
- 审核
- 重新发布

普通员工操作：

- 查看
- 编辑
- 删除
- 提交发布
- 驳回后重新编辑

状态为 `pending` 时，普通员工默认不能修改。

---

# 6. P04 新建 / 编辑内容

路由：

```text
/contents/new
/contents/:id/edit
```

字段：

- 标题
- 简介
- 分类
- 内容类型
- 上传文件 / 内容数据
- 发布目标（可选）

发布目标建议：

普通员工只选择后端允许的发布目标，不显示物理目录。

例如显示：

```text
PPT 发布区
PDF 发布区
内部页面
公共文件
```

不显示：

```text
/data/nginx/company/ppt/
```

按钮：

```text
保存草稿
提交发布
取消
```

管理员可增加：

```text
保存并发布
```

---

# 7. P05 内容详情 / 预览

路由：

```text
/contents/:id
```

显示：

- 标题
- 简介
- 类型
- 分类
- 创建人
- 创建时间
- 更新时间
- 审核状态
- 发布状态
- 发布目标
- 发布 URL
- 驳回原因
- 内容预览

操作：

普通员工：

- 编辑
- 提交发布
- 重新提交
- 打开已发布页面

管理员：

- 编辑
- 审核
- 发布
- 重新发布
- 打开已发布页面

---

# 8. P06 审核列表

仅管理员。

路由：

```text
/reviews
```

默认显示：

```text
review_status = pending
```

筛选：

- 关键词
- 内容类型
- 分类
- 提交人
- 提交日期

列表：

```text
标题
类型
分类
提交人
提交时间
目标发布位置
操作
```

操作：

- 查看
- 审核

---

# 9. P07 审核详情

仅管理员。

路由：

```text
/reviews/:contentId
```

页面内容：

- 内容基本信息
- 原始文件
- 内容预览
- 创建人
- 提交时间
- 目标发布位置
- 历史审核信息

按钮：

```text
审核通过并发布
驳回
```

驳回时填写：

```text
驳回原因
```

审核通过后前端只调用 API。

实际文件转换、目录写入、URL 生成由后端完成。

---

# 10. P08 发布配置

仅管理员。

路由：

```text
/settings/publish-targets
```

功能：

- 查看发布目标
- 新增发布目标
- 编辑发布目标
- 启用 / 禁用
- 删除

列表字段：

```text
名称
适用内容类型
发布目录
URL 根地址
状态
操作
```

新增 / 编辑字段：

```text
名称
内容类型
服务器发布根目录
URL 根地址
是否启用
```

例如：

```text
名称：PPT 发布区
类型：ppt
服务器目录：/data/company/presentation/
URL 根地址：https://internal.example.com/presentation/
```

注意：

普通员工永远看不到服务器物理路径。

---

# 11. P09 用户管理

仅管理员。

路由：

```text
/users
```

列表：

```text
用户名
姓名
角色
状态
创建时间
操作
```

功能：

- 新增
- 编辑
- 启用
- 禁用
- 删除

角色：

```text
管理员
普通员工
```

不需要单独的角色管理页面。

---

# 12. P10 日志

仅管理员。

路由：

```text
/logs
```

建议一个页面两个 Tab：

```text
操作日志
发布日志
```

## 操作日志

筛选：

- 用户
- 操作类型
- 日期

展示：

```text
时间
用户
操作
对象
说明
```

## 发布日志

筛选：

- 内容
- 发布状态
- 内容类型
- 日期

展示：

```text
时间
内容
发布目标
结果
访问 URL
失败原因
```

---

# 13. P11 内容检索

路由：

```text
/search
```

管理员和普通员工均可访问。

顶部：

```text
关键词搜索框
```

筛选：

```text
内容类型
分类
发布时间
```

搜索结果：

```text
标题
简介
内容类型
分类
发布时间
```

点击结果：

- 可以进入 `/contents/:id`
- 已发布内容提供“打开内容”按钮
- 也可以直接通过 `view_url` 打开最终发布内容

---

# 14. 内容查看

不强制增加独立后台页面。

后端返回：

```json
{
  "view_url": "https://internal.example.com/presentation/demo/"
}
```

前端根据实际需求：

```text
方式 A：新窗口打开
方式 B：iframe 内嵌
```

推荐第一阶段优先采用新窗口打开，降低前端复杂度。

---

# 15. 推荐前端菜单

## 管理员

```text
首页
内容管理
审核管理
内容检索
用户管理
发布配置
日志
退出
```

## 普通员工

```text
首页
我的内容
内容检索
退出
```

---

# 16. 推荐前端技术结构

示例：

```text
src/
├─ api/
│  ├─ auth.ts
│  ├─ users.ts
│  ├─ contents.ts
│  ├─ reviews.ts
│  ├─ publishTargets.ts
│  ├─ search.ts
│  └─ logs.ts
│
├─ pages/
│  ├─ Login/
│  ├─ Dashboard/
│  ├─ Contents/
│  ├─ Reviews/
│  ├─ Users/
│  ├─ PublishTargets/
│  ├─ Search/
│  └─ Logs/
│
├─ components/
│  ├─ ContentForm/
│  ├─ ContentStatus/
│  ├─ FileUploader/
│  ├─ ContentPreview/
│  └─ SearchFilter/
│
├─ router/
├─ store/
└─ utils/
```

前端框架可采用：

```text
React + TypeScript
```

或：

```text
Vue 3 + TypeScript
```

两者均可，不影响本文定义的 API。
