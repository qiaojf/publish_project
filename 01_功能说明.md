# 公司内部内容自动发布平台 — 功能说明

## 1. 项目目标

建设一个公司内部使用的内容自动发布平台。

核心流程：

```text
登录
  ↓
创建 / 上传内容
  ↓
普通员工提交发布申请
  ↓
管理员审核
  ├─ 驳回 → 员工修改后重新提交
  └─ 通过
       ↓
后端根据内容类型自动处理
       ↓
根据发布目标配置发布到指定目录
       ↓
生成访问 URL
       ↓
员工检索 / 查看内容
       ↓
记录操作日志与发布日志
```

系统采用前后端解耦架构：

```text
Frontend
  ↓ REST API
Backend API
  ↓
内容处理 / 审核 / 发布服务
  ↓
文件存储 / 发布目录 / Web Server
  ↓
Database
```

前端不直接操作服务器目录，也不包含发布逻辑。
所有发布规则、目录解析、文件转换和发布操作由后端负责。

---

# 2. 用户角色

系统只保留两个角色。

## 2.1 管理员

可以：

- 登录
- 管理全部用户
- 查看全部内容
- 新建 / 编辑 / 删除内容
- 直接提交内容
- 审核普通员工提交的发布申请
- 审核通过后触发自动发布
- 驳回发布申请
- 查看发布结果
- 重新发布失败内容
- 配置发布目标和发布目录
- 检索全部已发布内容
- 查看操作日志
- 查看发布日志

## 2.2 普通员工

可以：

- 登录
- 新建内容
- 上传内容
- 编辑自己创建的内容
- 删除自己未发布的内容
- 查看自己的内容
- 提交发布申请
- 查看审核状态
- 查看驳回原因
- 修改后重新提交
- 检索已发布内容
- 查看已发布内容

普通员工不能：

- 管理用户
- 审核内容
- 修改发布目录配置
- 查看系统级日志
- 修改其他员工内容

---

# 3. 功能模块

## 3.1 登录

功能：

- 用户名 + 密码登录
- 登录状态保持
- 获取当前用户信息
- 退出登录
- 根据角色控制页面和操作权限

---

# 3.2 用户管理

仅管理员使用。

功能：

- 用户列表
- 新增用户
- 修改用户
- 禁用用户
- 启用用户
- 删除用户
- 设置角色

角色固定为：

- `admin`
- `employee`

不做复杂角色和权限系统。

---

# 3.3 内容管理

管理员可以管理全部内容。

普通员工只能管理自己创建的内容。

支持的内容类型建议第一阶段包括：

- HTML 静态页面
- 动态页面
- PPT / PPTX
- PDF
- Word
- Excel
- 图片
- 普通文件

内容基础信息：

- 标题
- 简介
- 分类
- 内容类型
- 原始文件 / 内容数据
- 创建人
- 创建时间
- 更新时间
- 审核状态
- 发布状态
- 发布地址

主要功能：

- 新建内容
- 上传文件
- 编辑内容
- 删除内容
- 查看详情
- 内容预览
- 分类
- 查看审核状态
- 查看发布状态

---

# 3.4 发布申请

普通员工发布内容时，不直接执行正式发布。

流程：

```text
内容编辑完成
  ↓
提交发布
  ↓
状态 = 待审核
  ↓
管理员审核
```

提交后，在审核完成前原则上不允许继续修改。

如果需要修改：

- 管理员驳回后修改
- 或撤回申请后修改（第一版可选）

---

# 3.5 管理员审核

管理员增加审核功能。

审核状态：

- 草稿
- 待审核
- 审核通过
- 已驳回

审核操作：

- 查看待审核内容
- 预览内容
- 查看提交人
- 查看目标发布位置
- 审核通过
- 驳回
- 填写审核备注 / 驳回原因

审核通过后：

```text
审核通过
  ↓
创建发布任务
  ↓
执行自动发布
```

管理员自己创建的内容也建议走同一发布逻辑，但可直接审核通过并发布，不需要再次等待其他管理员审批。

---

# 3.6 自动发布

发布由后端统一处理。

流程：

```text
内容
  ↓
识别内容类型
  ↓
选择对应处理器
  ↓
生成可发布内容
  ↓
读取发布目标配置
  ↓
写入目标目录
  ↓
生成访问 URL
  ↓
更新发布状态
  ↓
记录发布日志
```

支持：

- 根据内容类型自动处理
- 自动发布到配置目录
- 自动生成 URL
- 发布成功
- 发布失败
- 失败原因记录
- 管理员重新发布

---

# 3.7 内容类型处理器

建议后端采用可扩展处理器方式。

```text
HTML      → HtmlPublisher
PPT/PPTX  → PptPublisher
PDF       → PdfPublisher
Word      → WordPublisher
Excel     → ExcelPublisher
Image     → ImagePublisher
File      → FilePublisher
Dynamic   → DynamicPagePublisher
```

第一阶段某些类型如果暂时不能转换为网页，可以先采用：

```text
文件复制到发布目录
+
生成统一查看 / 下载入口
```

以后再替换成具体转换器，不影响前端。

---

# 3.8 发布目标 / 发布目录配置

这是本项目需要保留的核心配置功能。

管理员可以维护多个发布目标，例如：

```text
内部静态页面
内部文档
PPT 页面
PDF 页面
图片资源
公共文件
```

每个发布目标配置：

- 名称
- 适用内容类型
- 服务器发布根目录
- URL 根地址
- 是否启用

示例：

```text
名称：PPT 发布目录
类型：ppt
目录：/data/company-site/presentation/
URL：https://internal.example.com/presentation/
```

内容发布时：

```text
publish_root + generated_path
```

得到物理文件位置。

同时：

```text
base_url + generated_path
```

得到最终访问 URL。

这样前端完全不依赖实际服务器路径。

后续服务器目录变化，只修改后台配置即可。

---

# 3.9 内容检索

管理员和普通员工均可使用。

只检索“允许当前用户访问且已经发布”的内容。

筛选条件：

- 关键词
- 内容类型
- 分类
- 发布时间

关键词第一阶段可以搜索：

- 标题
- 简介

如果后续需要文档正文全文检索，再单独增加搜索引擎。

搜索结果显示：

- 标题
- 类型
- 分类
- 简介
- 发布时间
- 发布人
- 查看入口

---

# 3.10 内容查看

根据内容类型进入不同查看方式。

```text
HTML
→ 直接打开页面

动态页面
→ 打开动态页面 URL

PPT
→ PPT Web Viewer

PDF
→ PDF Viewer

Word
→ 在线预览或下载

Excel
→ 在线预览或下载

图片
→ 图片查看

普通文件
→ 下载 / 浏览器支持时直接打开
```

统一由内容详情 API 返回：

```text
view_url
```

前端不需要知道实际服务器文件路径。

---

# 3.11 日志

第一阶段只保留两类日志。

## 操作日志

记录关键后台操作：

- 登录
- 新建内容
- 编辑内容
- 删除内容
- 提交审核
- 审核通过
- 审核驳回
- 用户新增 / 编辑 / 禁用 / 删除
- 修改发布目录

## 发布日志

记录：

- 内容
- 发布人 / 审核人
- 发布时间
- 内容类型
- 发布目标
- 发布目录
- 最终 URL
- 发布结果
- 失败原因
- 重新发布记录

---

# 4. 内容状态

建议不要设计过多状态。

## 审核状态 `review_status`

```text
draft            草稿
pending          待审核
approved         审核通过
rejected         已驳回
```

## 发布状态 `publish_status`

```text
unpublished      未发布
publishing       发布中
published        已发布
failed           发布失败
```

两套状态分开，避免逻辑混乱。

例如：

```text
review_status = approved
publish_status = failed
```

表示审核已经通过，但自动发布失败，可以由管理员重新发布。

---

# 5. 第一阶段不包含

为了保持项目简单，暂时不实现：

- 多级审批
- 自定义角色
- 复杂权限系统
- 内容版本管理
- 发布回滚
- 定时发布
- 定时下架
- 收藏
- 评论
- 点赞
- 消息通知
- 全文搜索引擎
- 部门组织架构
- 标签体系
- 多文件复杂内容包管理
- 工作流设计器

---

# 6. 第一阶段完整闭环

最终 MVP 只需要保证以下流程稳定：

```text
管理员创建用户
       ↓
员工登录
       ↓
员工上传 / 创建内容
       ↓
员工提交发布
       ↓
管理员审核
       ↓
审核通过
       ↓
后端自动处理文件
       ↓
按照后台配置的目录发布
       ↓
生成 URL
       ↓
员工搜索
       ↓
打开 / 查看内容
       ↓
系统完整记录操作与发布日志
```

只要以上流程完整，第一阶段即可投入内部试用。
